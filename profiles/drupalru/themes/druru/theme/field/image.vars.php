<?php

/**
 * An element #process callback for the image_image field type.
 *
 * Expands the image_image type to include the alt and title fields.
 */
function druru_field_widget_process($element, &$form_state, $form) {
  is_array(1);
}
